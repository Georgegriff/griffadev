<!DOCTYPE html>
<html data-theme="light" lang="en">
    <head>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <meta http-equiv="X-UA-Compatible" content="ie=edge"/>
        <title>{{ title or metadata.title }}</title>
        <meta name="description" content="{{ description or metadata.description }}">
        <!-- todo learn about feeds -->
        <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
        <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">


        <!-- todo preload right fonts, can we do this magically? -->
        <link rel="preload" as="font" type="font/woff2" href="/fonts/Red_Hat_Display-400-latin20.woff2" />
        <link rel="preload" as="font" type="font/woff2" href="/fonts/Red_Hat_Display-900-latin22.woff2" />
        <link rel="preload" as="font" type="font/woff2" href="/fonts/Literata-400-latin12.woff2"/>

        <link rel="preload" media="print" href="/fonts/fonts.css?{{ assetHash }}" onload="this.media='all'"/>

        <style>
            {% include "css/critical.css" %}
        </style>

        {# Add facility for pages to delare an array of critical styles #}
        {% if pageCriticalStyles %}
            {% for item in pageCriticalStyles %}
                <style>
                    {% include item %}
                </style>
            {% endfor %}
        {% endif %}
    </head>
    <body>
        <input aria-label="Switch theme colors" type="checkbox" id="theme" name="theme" class="visually-hidden">
        <div class="wrapper page-layout transition">
            {% include "partials/actionbar.html" %}
            <main id="main-content">
                {% include "partials/logo.html" %}
                <div class="main-content">
                 {% block content %}{% endblock %}
                </div>
            </main>
        </div>
        <script src="{{ assets.getPath("index.js")}}" type="module"></script>
    </body>
</html>