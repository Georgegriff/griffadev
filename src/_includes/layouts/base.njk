<!DOCTYPE html>
<html data-theme="light" lang="en">
    <head>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <meta http-equiv="X-UA-Compatible" content="ie=edge"/>
        <title>{{ title or metadata.title }}</title>
        <meta name="description" content="{{ description or metadata.description }}">
        <!-- todo learn about feeds -->
        <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
        <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">


        <!-- todo preload right fonts, can we do this magically? -->
        <link rel="preload" as="font" type="font/woff2" href="/fonts/Nunito-400-latin5.woff2" />
        <link rel="preload" as="font" type="font/woff2" href="/fonts/Nunito-600-latin10.woff2" />
        <link rel="preload" as="font" type="font/woff2" href="/fonts/Nunito-800-latin15.woff2"/>

        <link rel="preload" as="style" media="print" href="/fonts/fonts.css?{{ assetHash }}" onload="this.media='all'"/>
        <base href="/">
        <style>
            {% include "css/critical.css" %}
        </style>

        {# Add facility for pages to delare an array of critical styles #}
        {% if pageCriticalStyles %}
            {% for item in pageCriticalStyles %}
                <style>
                    {% include item %}
                </style>
            {% endfor %}
        {% endif %}
    </head>
    <body>
        <input aria-label="Switch theme colors" type="checkbox" id="theme" name="theme" class="visually-hidden">
        <div class="wrapper page-layout">
            {% include "partials/actionbar.html" %}
            <main id="main-content">
                <intersect-detect class="main-content">
                    {% include "partials/header.html" %}
                    {% block content %}{% endblock %}
                </intersect-detect>
            </main>
        </div>
        <div class="filler"></div>
        <!-- TODO  split  js into pages -->
        <script src="{{ assets.getPath("index.js")}}" type="module"></script>
    </body>
</html>